# Dependencies
node_modules/

# Test and coverage output
coverage/
.nyc_output/
test-results/
playwright-report/
output/playwright/

# Build and cache output
dist/
build/
.cache/
.parcel-cache/
.turbo/
.vite/
*.tsbuildinfo

# Local Harness and Amber runtime state
.harness/
.amber/
.amber-legacy-harness-backup-*/

# Claude Code local settings (personal config, not team-shared)
.claude/settings.local.json
.claude/worktrees/
.claude/plans/

# Test temp output
tests/.tmp/

# Local tool / screenshot artifacts
.grok/
.mimocode/
.workbuddy/
.playwright-cli/
apps/web/.playwright-cli/
apps/web/.tanstack/
apps/web/screenshots/

# Local architecture review input (F016 acceptance uses the tracked suffixed report)
output/adr-architecture-assessment.md

# Local design-skill (impeccable) working artifacts
.impeccable/

# Package dry-run and local archive output
npm-pack-dry-run.json
*.tgz

# Local scratch dirs (agent transcripts, terminal state, archived drafts) —
# not version-controlled
agent-tools/
terminals/
.archive/
.scratch/

# New Superpowers workflow artifacts (historical tracked records remain versioned)
docs/superpowers/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.*
!.env.example

# npm auth (contains GitHub tokens — use .github/npmrc-github-packages as template)
.npmrc

# OS and editor files
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/
.qoder/
*.swp
*.swo
*~

# Vite writes a temp file next to a TS/MTS config while loading it
*.timestamp-*.mjs

# Local working files (notes, checklists, scratch data)
notes.md
MEMORY.md
.release-checklist.md
session-handoff.md
tasks/
# Governed shared memory (ADR-0018) — tracked by git by default, unlike the
# local working files above; book entries via `amber memory book`.
!/MEMORY.md
feature_list.json
!/feature_list.json
问题清单.md

# Local regression-test scratch (go-stock analysis reports and ad-hoc test scripts)
test-reports/
test-workflows/
