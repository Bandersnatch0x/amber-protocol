# Packet A - RED Tests

Implementer task:

- Add CLI behavior tests for `adoption bundle`.
- Confirm tests fail because command behavior is missing.

Spec review must confirm:

- Tests cover required bundle files.
- Tests cover manifest contents.
- Tests cover no-overwrite output-dir behavior.

Quality review must confirm:

- Tests use public CLI only.
- Fixtures remain small and local.
