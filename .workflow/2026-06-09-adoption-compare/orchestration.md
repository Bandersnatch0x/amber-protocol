# Orchestration

1. Add failing CLI tests for directory auto-selection, explicit markdown output, and no-overwrite behavior.
2. Implement the smallest parser/comparator needed to pass tests.
3. Smoke-test against `docs/examples/adoptions`.
4. Update README and examples docs.
5. Run full verification and close the workflow.

Subagents are simulated with isolated packet notes under `results/`.
