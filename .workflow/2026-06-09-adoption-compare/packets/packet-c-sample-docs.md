# Packet C - sample Smoke And Docs

Run compare against:

- `docs/examples/adoptions` via `--reports-dir`;
- explicit `--base` and `--head` sample report files with markdown output.

Update docs with command examples and safety boundary.
