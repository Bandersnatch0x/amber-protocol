# Packet B - Implementation

Implement:

- argument parsing for `--base` and `--head`;
- report comparison parser;
- core compare helper;
- explicit markdown diff writer with no-overwrite behavior;
- CLI route and text output.

Keep comparison read-only unless `--output` is explicitly provided.
