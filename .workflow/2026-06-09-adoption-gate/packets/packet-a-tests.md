# Packet A - RED Tests

Implementer task:

- Add behavior-first CLI tests for `adoption gate`.
- Confirm tests fail because command behavior is missing.

Spec review must confirm:

- Tests cover single report gate and reports-dir latest selection.
- Tests cover markdown output no-overwrite behavior.
- Tests assert read-only behavior.

Quality review must confirm:

- Tests use public CLI only.
- Fixtures are small and local.
