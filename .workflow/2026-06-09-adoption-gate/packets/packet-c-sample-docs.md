# Packet C - sample Smoke And Docs

Implementer task:

- Run `adoption gate` against sample example reports.
- Update README and examples docs.

Spec review must confirm:

- sample target remains untouched.
- Docs describe conservative `wait` behavior honestly.

Quality review must confirm:

- Examples are copy-pasteable from documented working directories.
