# Packet A: Tests

Objective: define adoption report list/index behavior before implementation.

Expected output:

- CLI test for `adoption list --reports-dir`.
- CLI test for `adoption index --reports-dir --output`.
- Index test confirms no overwrite.

