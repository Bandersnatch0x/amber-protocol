# Packet A: RED Tests

## Task

Add failing CLI tests for `adoption selected-files`.

## Requirements

- Writes a markdown proposal from a bundle fixture.
- Separates required and optional files.
- Supports repeatable `--include`.
- Rejects unknown included files.
- Refuses overwrite.

## Boundary

Temporary fixtures only. No target writes.

