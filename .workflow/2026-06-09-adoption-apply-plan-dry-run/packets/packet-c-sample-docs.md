# Packet C: sample Smoke And Docs

## Task

Generate a sample apply-plan dry-run artifact and update docs.

## Requirements

- Write under `docs/examples/`.
- Keep sample untouched.
- Document that `--dry-run` is required in V1.

