# Packet A: Tests

Objective: define the adoption report behavior before implementation.

Expected output:

- A CLI test that fails because `adoption report` is not implemented.
- Assertions that report generation is read-only for target root files and includes the core adoption sections.

