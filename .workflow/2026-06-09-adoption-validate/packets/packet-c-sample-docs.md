# Packet C - sample Smoke And Docs

Run `adoption validate` against:

- `docs/examples/adoptions`;
- `docs/examples/adoptions-index.md`.

Update README and examples docs with the new command and safety boundary.
