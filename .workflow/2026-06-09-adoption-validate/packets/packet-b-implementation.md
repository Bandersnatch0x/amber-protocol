# Packet B - Implementation

Implement:

- argument parsing for `--index`;
- core validation helper for adoption report artifacts;
- CLI route for `adoption validate`;
- human-readable output for validation results.

Keep implementation read-only.
