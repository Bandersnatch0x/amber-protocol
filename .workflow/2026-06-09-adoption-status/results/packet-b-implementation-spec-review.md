# Packet B - Spec Review

Status: approved.

- Status is read-only unless `--output` is explicitly provided.
- Index validation, gate decision, compare summary, blockers, and next safe action are included.
- `--output` refuses overwrite with structured errors.
