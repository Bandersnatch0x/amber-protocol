# Packet A - RED Tests

Implementer task:

- Add CLI behavior tests for `adoption status`.
- Confirm tests fail because command behavior is missing.

Spec review must confirm:

- Tests cover read-only summary.
- Tests cover optional index validation.
- Tests cover markdown output no-overwrite.

Quality review must confirm:

- Tests use public CLI only.
- Fixtures remain small and local.
