# Packet C: StockAgents Smoke And Docs

## Task

Use the new CLI against `docs/examples/stockagents-adoption-bundle/` and update docs.

## Requirements

- Write a non-conflicting StockAgents next-actions artifact under `docs/examples/`.
- Document the command in `README.md`.
- Document the generated artifact in `docs/examples/README.md`.
- Verify StockAgents root remains unchanged.

