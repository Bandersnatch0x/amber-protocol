# Packet C: sample Smoke And Docs

## Task

Use the new CLI against `docs/examples/sample-adoption-bundle/` and update docs.

## Requirements

- Write a non-conflicting sample next-actions artifact under `docs/examples/`.
- Document the command in `README.md`.
- Document the generated artifact in `docs/examples/README.md`.
- Verify sample root remains unchanged.

