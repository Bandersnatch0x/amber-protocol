# Packet C: sample Smoke And Docs

## Task

Generate a sample decision-record example with explicit decision states and update docs.

## Requirements

- Write under `docs/examples/`.
- Keep sample untouched.
- Document `--decision` syntax.

