---
id: hooks
title: "amber hooks"
sidebar_label: "hooks"
description: "Manage the opt-in git pre-commit governance guard."
---

import CommandBlock from '@site/src/components/CommandBlock';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber hooks

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>expert</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Manage the opt-in git pre-commit governance guard.

## Usage

```bash
amber hooks <check|install|uninstall|status> --target <repo> | amber hooks breadcrumb <print|install|uninstall|status> --target <repo> [--format json|text] [--platform claude] [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber hooks --target path/to/repo"
  expectedOutput="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>breadcrumb</code> | Subcommand action for <code>hooks</code> |
| <code>check</code> | Subcommand action for <code>hooks</code> |
| <code>install</code> | Subcommand action for <code>hooks</code> |
| <code>status</code> | Subcommand action for <code>hooks</code> |
| <code>uninstall</code> | Subcommand action for <code>hooks</code> |

## Command Details

```text
Manage the opt-in git pre-commit governance guard.

Subcommands:
  install [--warn-only] [--force]
        Install a .git/hooks/pre-commit guard. --warn-only never blocks; --force overwrites a foreign hook.
  uninstall   Remove the Amber guard (restores any backed-up hook).
  status      Report whether the guard is installed and its mode.
  check [--warn-only]
        Run the governance checks now (this is what the hook invokes).

  breadcrumb print [--format json|text]
        Render the per-turn <amber-workflow-state> block (read-only; default json envelope).
  breadcrumb install [--platform claude]
        Opt-in merge of the breadcrumb command into .claude/settings.json hooks.UserPromptSubmit.
  breadcrumb uninstall
        Remove the Amber-managed breadcrumb entry; foreign entries are preserved.
  breadcrumb status
        Report whether the breadcrumb hook is installed (and echo its command).

The breadcrumb is opt-in and never auto-installed (amber init does not add it).
The hook only reads governance metadata; it never runs project build/test commands.
Bypass once with: AMBER_SKIP_HOOKS=1 git commit ... (breadcrumb print is silent too).

Examples:
  amber hooks install --target .
  amber hooks status --target .
  amber hooks check --target .
  amber hooks uninstall --target .
  amber hooks breadcrumb print --target . --format text
  amber hooks breadcrumb install --target . --platform claude
  amber hooks breadcrumb status --target .
```

## Examples

```bash
amber hooks install --target .
```

```bash
amber hooks status --target .
```

```bash
amber hooks check --target .
```

```bash
amber hooks uninstall --target .
```

```bash
amber hooks breadcrumb print --target . --format text
```

```bash
amber hooks breadcrumb install --target . --platform claude
```

```bash
amber hooks breadcrumb status --target .
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
