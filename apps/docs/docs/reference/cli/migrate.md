---
id: migrate
title: "amber migrate"
sidebar_label: "migrate"
description: "Backfill version metadata in recognized Amber JSON artifacts, or migrate"
---

import CommandBlock from '@site/src/components/CommandBlock';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber migrate

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>expert</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Backfill version metadata in recognized Amber JSON artifacts, or migrate

## Usage

```bash
amber migrate --target <repo> [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber migrate --target path/to/repo"
  expectedOutput="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>state</code> | Subcommand action for <code>migrate</code> |
| <code>wiki</code> | Subcommand action for <code>migrate</code> |

## Command Details

```text
Backfill version metadata in recognized Amber JSON artifacts, or migrate
legacy state and wiki naming to the Amber Protocol layout.

Subcommands:
  state        Merge legacy .harness state into .amber without overwriting.
  wiki         Rename docs/wiki/agent/harness.md to amber.md and fix links.
  (none)       Backfill recognized artifacts in .amber, routes, and workflow-packs.

Options:
  --archive-legacy  Rename .harness to a timestamped backup after clean state migration.
  --dry-run         Preview changes without writing files (artifact mode).

Examples:
  amber migrate --target <path>
  amber migrate state --target <path>
  amber migrate wiki --target <path>
```

## Examples

```bash
amber migrate --target <path>
```

```bash
amber migrate state --target <path>
```

```bash
amber migrate wiki --target <path>
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
