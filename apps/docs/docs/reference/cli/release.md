---
id: release
title: "amber release"
sidebar_label: "release"
description: "Prepare governed release candidates (F053 T1)."
---

import CommandBlock from '@site/src/components/CommandBlock';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber release

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Prepare governed release candidates (F053 T1).

## Usage

```bash
amber release <prepare|authorize|deploy|rollback|transactions|status|receipts|show|list> --target <repo> [--json]
       amber release prepare --target <repo> --id <release-id> --commit <40-hex> --change-artifact <type>:<identity>@<rev> --evidence-item <evidence-id> --review-logic <evidence-id> --review-security <evidence-id> --review-spec <evidence-id> --environment development|staging|production --release-policy <identity>@<rev> --runner <runner-id> --runner-version <version> --capability <name> --capability-version <version> --credential none|scoped --rollback <evidence-id> [--json]
       amber release authorize --target <repo> --id <release-id> [--approval <id>] [--decision-identity <identity>] [--body <markdown>] [--trace <decides:...>] [--scope <scope>] [--rehearsal <evidence-id>] [--branch-protection <evidence-id>] [--code-owner <identity>@<rev>] [--release-manager <identity>@<rev>] [--release-gate-index <n>] [--environment-gate-index <n>] [--json]
       amber release deploy --target <repo> --id <release-id> --request-hash <sha256:...> [--json]
       amber release rollback --target <repo> --id <release-id> --request-hash <sha256:...> [--json]
       amber release transactions --target <repo> [--id <release-id>] [--json]
       amber release status --target <repo> --id <release-id> [--json]
       amber release receipts --target <repo> --id <release-id> [--json]
       amber release show --target <repo> --id <release-id> [--json]
       amber release list --target <repo> [--environment <env>] [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber release --target path/to/repo"
  expectedOutput="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>authorize</code> | Subcommand action for <code>release</code> |
| <code>deploy</code> | Subcommand action for <code>release</code> |
| <code>list</code> | Subcommand action for <code>release</code> |
| <code>prepare</code> | Subcommand action for <code>release</code> |
| <code>receipts</code> | Subcommand action for <code>release</code> |
| <code>rollback</code> | Subcommand action for <code>release</code> |
| <code>show</code> | Subcommand action for <code>release</code> |
| <code>status</code> | Subcommand action for <code>release</code> |
| <code>transactions</code> | Subcommand action for <code>release</code> |

## Command Details

```text
Prepare governed release candidates (F053 T1).
A candidate immutably binds one exact Change (commit + committed Artifact
revisions), recorded Evidence, per-axis Review findings (logic, security,
spec compliance — Evidence references, never approvals), the target
environment, a versioned release Policy artifact, one registered F052
Runner capability pin, the credentials class, and a rollback plan into a
canonical releaseHash. Preparation is a governance write: it never deploys
and touches no git state; any drift invalidates downstream authorization.

Subcommands:
  prepare --id <release-id> --commit <40-hex> --change-artifact <type>:<identity>@<rev>
        [--change-artifact ...] --evidence-item <evidence-id> [--evidence-item ...]
        --review-logic <evidence-id> --review-security <evidence-id>
        --review-spec <evidence-id> --environment <development|staging|production>
        --release-policy <identity>@<rev> --runner <runner-id> --runner-version <v>
        --capability <name> --capability-version <v> --credential <none|scoped>
        --rollback <evidence-id>
  authorize --id <release-id>
        staging: --approval <id> --decision-identity <identity> --body <markdown>
        [--trace decides:<type>:<identity>] [--scope <scope>] --rehearsal <evidence-id>
        production: --branch-protection <evidence-id> --code-owner <identity>@<rev>
        --release-manager <identity>@<rev> --release-gate-index <n>
        --environment-gate-index <n>
  deploy --id <release-id> --request-hash <sha256:...>
  rollback --id <release-id> --request-hash <sha256:...>
  transactions [--id <release-id>]
  status --id <release-id>
  receipts --id <release-id>
  show --id <release-id>
  list [--environment <env>]

Examples:
  amber release prepare --target . --id release/web-42 --commit <40-hex> --change-artifact spec:spec/login@2 --evidence-item evidence/test-run --review-logic evidence/review-logic --review-security evidence/review-security --review-spec evidence/review-spec --environment staging --release-policy policy/release@1 --runner runner/ci --runner-version 1.0.0 --capability deploy.staging-web --capability-version 1 --credential scoped --rollback evidence/rollback-plan --json
  amber release show --target . --id release/web-42 --json
```

## Examples

```bash
amber release prepare --target . --id release/web-42 --commit <40-hex> --change-artifact spec:spec/login@2 --evidence-item evidence/test-run --review-logic evidence/review-logic --review-security evidence/review-security --review-spec evidence/review-spec --environment staging --release-policy policy/release@1 --runner runner/ci --runner-version 1.0.0 --capability deploy.staging-web --capability-version 1 --credential scoped --rollback evidence/rollback-plan --json
```

```bash
amber release show --target . --id release/web-42 --json
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
