---
id: workflow
title: "amber workflow"
sidebar_label: "workflow"
description: "Assess agent-workflow effectiveness across five dimensions, separate from"
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber workflow

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>expert</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Assess agent-workflow effectiveness across five dimensions, separate from

## Usage

```bash
amber workflow <assess|findings|plan|compare> --target <repo>
       amber workflow assess --target <repo> [--format json|markdown] [--output-dir <path>] [--no-sessions]
       amber workflow findings --target <repo> --report <path>
       amber workflow plan --target <repo> --report <path> --finding <id>
       amber workflow compare --target <repo> --baseline <path> --current <path>
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber workflow --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>assess</code> | Subcommand action for <code>workflow</code> |
| <code>compare</code> | Subcommand action for <code>workflow</code> |
| <code>findings</code> | Subcommand action for <code>workflow</code> |
| <code>plan</code> | Subcommand action for <code>workflow</code> |

## Command Details

```text
Assess agent-workflow effectiveness across five dimensions, separate from
governance readiness. Read-only by default. See ADR-0008.

Subcommands:
  assess [--format json|markdown] [--output-dir <path>] [--no-sessions]
        Produce a workflow-effectiveness report. Sessions (amber-native)
        are included by default; --no-sessions emits a repository-only
        baseline. --format json is the default output. --output-dir writes
        the report to a file instead of stdout (assess only).
  findings   (P2) List findings from a prior report.
  plan --report <path> --finding <id> [--dry-run]
        Bridge a finding to a dry-run amber plan draft (review before applying).
  compare    (P3) Compare two reports across schema versions.

Notes:
  --json has no effect on workflow output; use --format to pick json/markdown.

Examples:
  amber workflow assess --target . --format json
  amber workflow assess --target . --format markdown --output-dir docs/quality/
  amber workflow assess --target . --no-sessions
```

## Examples

```bash
amber workflow assess --target . --format json
```

```bash
amber workflow assess --target . --format markdown --output-dir docs/quality/
```

```bash
amber workflow assess --target . --no-sessions
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
