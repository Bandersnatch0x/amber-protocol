---
id: memory
title: "amber memory"
sidebar_label: "memory"
description: "Govern the memory write-back pipeline (Governed Memory Layer): request, ingest,"
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber memory

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>expert</code> • Classification: <code>Governed Command</code>
</div>

## Summary

Govern the memory write-back pipeline (Governed Memory Layer): request, ingest,

## Usage

```bash
amber memory <request|ingest|approve|book|abandon|status> [--target <repo>] [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber memory --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>abandon</code> | Subcommand action for <code>memory</code> |
| <code>approve</code> | Subcommand action for <code>memory</code> |
| <code>book</code> | Subcommand action for <code>memory</code> |
| <code>conversion</code> | Subcommand action for <code>memory</code> |
| <code>escape-hatch</code> | Subcommand action for <code>memory</code> |
| <code>gamma</code> | Subcommand action for <code>memory</code> |
| <code>ingest</code> | Subcommand action for <code>memory</code> |
| <code>ratification</code> | Subcommand action for <code>memory</code> |
| <code>request</code> | Subcommand action for <code>memory</code> |
| <code>status</code> | Subcommand action for <code>memory</code> |

## Command Details

```text
Govern the memory write-back pipeline (Governed Memory Layer): request, ingest,
approve, book, and abandon memory entries, plus a read-only status projection.
Amber never writes knowledge docs itself — humans curate MEMORY.md; Amber registers.

Subcommands:
  request    Produce a memory request (trigger / conversion / escape-hatch / ratification pre-nomination).
  ingest     Mechanically admit request entries into the registry as proposals (all-or-nothing).
  approve    Human, entry-level approval gate (--entry-id, --decision approve|reject, --reason).
  book       Register a MEMORY.md surface hash and promote booked entries to active.
             --ratify --claim <text> ratifies a human direct edit directly (γ-free).
  abandon    Explicit human abandon of a request or entry (terminal ledger marker).
  status     Read-only three-section projection (entries / gamma / alpha).

Boundary: Amber detects, admits, and registers; humans curate MEMORY.md and approve.

Examples:
  amber memory status --target . --json
  amber memory approve --target . --entry-id <id> --decision approve
  amber memory abandon --target . --entry <id>
```

## Examples

```bash
amber memory status --target . --json
```

```bash
amber memory approve --target . --entry-id <id> --decision approve
```

```bash
amber memory abandon --target . --entry <id>
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
