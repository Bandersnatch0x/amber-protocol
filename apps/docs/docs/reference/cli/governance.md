---
id: governance
title: "amber governance"
sidebar_label: "governance"
description: "Create, inspect, and report governance controls for a target repository."
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber governance

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Governed Command</code>
</div>

## Summary

Create, inspect, and report governance controls for a target repository.

## Usage

```bash
amber governance <docs|evidence|policy|audit|readiness|report|standards|rules> [--target <repo>] [--json]
       amber governance report --target <repo> [--output <file> --confirm] [--json]
       amber governance rules <init|inspect|check> --target <repo> [--command <cmd>]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="read-only"
  command="amber governance --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>audit</code> | Subcommand action for <code>governance</code> |
| <code>docs</code> | Subcommand action for <code>governance</code> |
| <code>evidence</code> | Subcommand action for <code>governance</code> |
| <code>policy</code> | Subcommand action for <code>governance</code> |
| <code>readiness</code> | Subcommand action for <code>governance</code> |
| <code>report</code> | Subcommand action for <code>governance</code> |
| <code>rules</code> | Subcommand action for <code>governance</code> |
| <code>standards</code> | Subcommand action for <code>governance</code> |

## Command Details

```text
Create, inspect, and report governance controls for a target repository.

Subcommands:
  docs         Generate governance documents (CODE_OF_CONDUCT.md, CONTRIBUTING.md, GOVERNANCE.md).
  evidence     Export governance evidence from sessions or tasks.
  policy       Show governance policy (defaults and overrides).
  audit        Generate comprehensive audit report with policy, sessions, and executions.
  readiness    Report local governance readiness for higher-autonomy agent work.
  report       Score the delivery loop and emit structured next actions.

Examples:
  amber governance docs --target path/to/repo
  amber governance docs --target path/to/repo --json
  amber governance evidence --session <id> --output evidence.md
  amber governance evidence --task <id> --output evidence.md --json
  amber governance policy --target path/to/repo
  amber governance policy --target path/to/repo --json
  amber governance audit --target path/to/repo --output audit.md
  amber governance audit --target path/to/repo --output audit.md --since 2025-01-01
  amber governance audit --target path/to/repo --output audit.md --json
  amber governance readiness --target path/to/repo --json
  amber governance readiness --target path/to/repo --output docs/governance-readiness.md
  amber governance report --target path/to/repo
  amber governance report --target path/to/repo --output docs/governance-report.md --confirm
```

## Examples

```bash
amber governance docs --target path/to/repo
```

```bash
amber governance docs --target path/to/repo --json
```

```bash
amber governance evidence --session <id> --output evidence.md
```

```bash
amber governance evidence --task <id> --output evidence.md --json
```

```bash
amber governance policy --target path/to/repo
```

```bash
amber governance policy --target path/to/repo --json
```

```bash
amber governance audit --target path/to/repo --output audit.md
```

```bash
amber governance audit --target path/to/repo --output audit.md --since 2025-01-01
```

```bash
amber governance audit --target path/to/repo --output audit.md --json
```

```bash
amber governance readiness --target path/to/repo --json
```

```bash
amber governance readiness --target path/to/repo --output docs/governance-readiness.md
```

```bash
amber governance report --target path/to/repo
```

```bash
amber governance report --target path/to/repo --output docs/governance-report.md --confirm
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
