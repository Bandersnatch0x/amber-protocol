---
id: phase
title: "amber phase"
sidebar_label: "phase"
description: "Manage Phase 0-4 gate evidence, promotion, and rollback (#168)."
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber phase

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Manage Phase 0-4 gate evidence, promotion, and rollback (#168).

## Usage

```bash
amber phase --target <repo> [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber phase --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>evidence</code> | Subcommand action for <code>phase</code> |
| <code>invariants</code> | Subcommand action for <code>phase</code> |
| <code>promote</code> | Subcommand action for <code>phase</code> |
| <code>rollback</code> | Subcommand action for <code>phase</code> |
| <code>transitions</code> | Subcommand action for <code>phase</code> |
| <code>validate</code> | Subcommand action for <code>phase</code> |

## Command Details

```text
Manage Phase 0-4 gate evidence, promotion, and rollback (#168).
Each phase gate requires complete deterministic evidence; promotion
requires explicit authorization; rollback requires a checkpoint
(destructive rollback is impossible) and records append-only lineage.

Subcommands:
  evidence --phase <phase-0..phase-4>  Show the gate evidence.
  validate --phase <phase>  Check evidence completeness.
  promote --phase <phase> --auth <authorization>
        Promote with complete evidence + explicit authorization.
  rollback --phase <phase> --checkpoint <id> [--reason <text>]
        Roll back to a checkpoint (never destructive).
  transitions              List append-only lineage.
  invariants               Check invariant non-regression.

Examples:
  amber phase validate --phase phase-0 --target . --json
  amber phase promote --phase phase-0 --auth human-approve --target . --json
```

## Examples

```bash
amber phase validate --phase phase-0 --target . --json
```

```bash
amber phase promote --phase phase-0 --auth human-approve --target . --json
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
