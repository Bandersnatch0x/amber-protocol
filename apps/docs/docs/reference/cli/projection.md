---
id: projection
title: "amber projection"
sidebar_label: "projection"
description: "Manage rebuildable read-only projections (ADR-0019 D5; ADR-0012 amendment):"
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber projection

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>expert</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Manage rebuildable read-only projections (ADR-0019 D5; ADR-0012 amendment):

## Usage

```bash
amber projection --target <repo> [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="read-only"
  command="amber projection --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>invalidate</code> | Subcommand action for <code>projection</code> |
| <code>list</code> | Subcommand action for <code>projection</code> |
| <code>rebuild</code> | Subcommand action for <code>projection</code> |
| <code>status</code> | Subcommand action for <code>projection</code> |
| <code>strict-query</code> | Subcommand action for <code>projection</code> |

## Command Details

```text
Manage rebuildable read-only projections (ADR-0019 D5; ADR-0012 amendment):
Governance Graph, Governed Knowledge Base, and Visualization Workbench.
Projections are derived from canonical Amber artifacts and are never canonical
authority — rebuild() regenerates from canonical state at any time.

Subcommands:
  rebuild --type <governance-graph|knowledge-base|visualization-workbench>
        Rebuild from canonical artifacts: context pages (.amber/context/pages/),
        plus committed Canonical Artifact revisions for the Governance Graph.
  status --type <type>  Report projection state: missing, current, or drifted.
  strict-query --scope <node> --checkpoint <sha256> --projection-version 1
        --limit <n> --sort id --depth <0|1> [--cursor <cursor>]
        Strict Governance Graph read: exact scope/checkpoint/version,
        expiring cursor, and stale dependency refusal.
  invalidate --subject <scope> --dependency <type:identity[@revision]> --reason <text>
        Append a scoped staleness receipt; history is never rewritten.
  list                  List all projections and their status.

Examples:
  amber projection rebuild --type governance-graph --target . --json
  amber projection status --type knowledge-base --target . --json
```

## Examples

```bash
amber projection rebuild --type governance-graph --target . --json
```

```bash
amber projection status --type knowledge-base --target . --json
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
