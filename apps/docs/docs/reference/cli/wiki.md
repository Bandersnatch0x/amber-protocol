---
id: wiki
title: "amber wiki"
sidebar_label: "wiki"
description: "Create missing Wiki starter files, skip existing files, then validate links. Supports --dry-run."
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber wiki

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Create missing Wiki starter files, skip existing files, then validate links. Supports --dry-run.

## Usage

```bash
amber wiki --target <repo> [--json] [--dry-run]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber wiki --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>knowledge</code> | Subcommand action for <code>wiki</code> |

## Command Details

```text
Create missing Wiki starter files, skip existing files, then validate links. Supports --dry-run.

Subcommands:
  (none)             Scaffold the base wiki skeleton (idempotent).
  knowledge          Knowledge Plan + Structured Knowledge Base (declarative plan format).
    knowledge plan        Pre-flight inspection + propose or update the plan.
    knowledge scaffold    Scaffold docs/wiki/knowledge-plan.json (or --yaml).
    knowledge inspect     Dump the loaded plan.
    knowledge report      Coverage report against declared documents + notes + cards.
    knowledge validate    Schema validation of the plan.
    knowledge build       Materialize structured knowledge pages under docs/wiki/knowledge/.

Examples:
  amber wiki knowledge plan --target .
  amber wiki knowledge scaffold --target . --yaml
  amber wiki knowledge build --target .
  amber wiki knowledge report --target .
```

## Examples

```bash
amber wiki knowledge plan --target .
```

```bash
amber wiki knowledge scaffold --target . --yaml
```

```bash
amber wiki knowledge build --target .
```

```bash
amber wiki knowledge report --target .
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
