---
id: execution
title: "amber execution"
sidebar_label: "execution"
description: "Validate execution boundaries and integration contracts."
---

import CommandBlock from '@site/src/components/CommandBlock';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber execution

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>expert</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Validate execution boundaries and integration contracts.

## Usage

```bash
amber execution --target <repo> [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber execution --target path/to/repo"
  expectedOutput="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>readiness</code> | Subcommand action for <code>execution</code> |
| <code>validate-integration</code> | Subcommand action for <code>execution</code> |

## Command Details

```text
Validate execution boundaries and integration contracts.

Subcommands:
  validate-integration  Validate integration contract structure and hardstops.
  readiness             Review plan for execution boundary violations.

Examples:
  amber execution validate-integration --contract path/to/contract.json --json
  amber execution readiness --plan path/to/plan.md --target path/to/repo --json
```

## Examples

```bash
amber execution validate-integration --contract path/to/contract.json --json
```

```bash
amber execution readiness --plan path/to/plan.md --target path/to/repo --json
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
