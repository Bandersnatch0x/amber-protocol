---
id: loop
title: "amber loop"
sidebar_label: "loop"
description: "Inspect loop contracts, write dry-run ledger previews, and record manual loop evidence without live scheduling."
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber loop

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Governed Command</code>
</div>

## Summary

Inspect loop contracts, write dry-run ledger previews, and record manual loop evidence without live scheduling.

## Usage

```bash
amber loop <inspect|recommend|run|approve|verify-ledger|record|status|validate-loop> [--target <repo>] [--file <pack>] [--contract <id>] [--dry-run|--execute] [--reviewer <name>] [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="read-only"
  command="amber loop --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>approve</code> | Subcommand action for <code>loop</code> |
| <code>inspect</code> | Subcommand action for <code>loop</code> |
| <code>recommend</code> | Subcommand action for <code>loop</code> |
| <code>record</code> | Subcommand action for <code>loop</code> |
| <code>run</code> | Subcommand action for <code>loop</code> |
| <code>status</code> | Subcommand action for <code>loop</code> |
| <code>validate-loop</code> | Subcommand action for <code>loop</code> |
| <code>verify-ledger</code> | Subcommand action for <code>loop</code> |

## Command Details

```text
Inspect loop contracts, write dry-run ledger previews, and record manual loop evidence without live scheduling.
Loop status accepts one ledger JSON file or a directory and reports bounded no-progress signals without executing anything.

Examples:
  amber loop inspect --file workflow-packs/safe-amber-bootstrap.pack.json --contract daily-amber-triage --json
  amber loop recommend --target . --goal "continuous improvement" --json
  amber loop run --file workflow-packs/safe-amber-bootstrap.pack.json --contract daily-amber-triage --dry-run --output .amber/loops/daily-amber-triage/ledger-preview.json --json
  amber loop record --file workflow-packs/safe-amber-bootstrap.pack.json --contract daily-amber-triage --trigger-source manual --stop-reason reviewer-gate-required --output .amber/loops/daily-amber-triage/manual-ledger.json --json
  amber loop status --ledger .amber/loops/daily-amber-triage/manual-ledger.json --json
  amber loop status --ledger .amber/loops/daily-amber-triage/history --json
  amber loop validate-loop --contract path/to/contract.json --json
```

## Examples

```bash
amber loop inspect --file workflow-packs/safe-amber-bootstrap.pack.json --contract daily-amber-triage --json
```

```bash
amber loop recommend --target . --goal "continuous improvement" --json
```

```bash
amber loop run --file workflow-packs/safe-amber-bootstrap.pack.json --contract daily-amber-triage --dry-run --output .amber/loops/daily-amber-triage/ledger-preview.json --json
```

```bash
amber loop record --file workflow-packs/safe-amber-bootstrap.pack.json --contract daily-amber-triage --trigger-source manual --stop-reason reviewer-gate-required --output .amber/loops/daily-amber-triage/manual-ledger.json --json
```

```bash
amber loop status --ledger .amber/loops/daily-amber-triage/manual-ledger.json --json
```

```bash
amber loop status --ledger .amber/loops/daily-amber-triage/history --json
```

```bash
amber loop validate-loop --contract path/to/contract.json --json
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
