---
id: feature
title: "amber feature"
sidebar_label: "feature"
description: "Add, list, remove features in feature_list.json, record verification evidence, and book feature paths."
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber feature

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Standard Command</code>
</div>

## Summary

Add, list, remove features in feature_list.json, record verification evidence, and book feature paths.

## Usage

```bash
amber feature --target <repo> [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="governed-write"
  command="amber feature --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>add</code> | Subcommand action for <code>feature</code> |
| <code>evidence</code> | Subcommand action for <code>feature</code> |
| <code>list</code> | Subcommand action for <code>feature</code> |
| <code>paths</code> | Subcommand action for <code>feature</code> |
| <code>remove</code> | Subcommand action for <code>feature</code> |
| <code>verify</code> | Subcommand action for <code>feature</code> |

## Command Details

```text
Add, list, remove features in feature_list.json, record verification evidence, and book feature paths.

Subcommands:
  add    --id <id> --title <text> [--priority <n>] [--area <area>] [--behavior <text>] [--verify <step>...] [--paths <p,p> | --path <p>]...
         Register a new feature in feature_list.json. To be doctor-valid, pass --area, --behavior, and at least one --verify.
  list
         List all registered features with status.
  remove --id <id>
         Remove a feature from feature_list.json.
  verify --feature <id> [--command <cmd>] [--result <text>] [--notes <text>]
         Record verification evidence for a feature.
  evidence --feature <id>
         List all recorded evidence for a feature.
  paths  --feature <id> [--path <p>]...
         Book paths onto a feature (append-only, comma-splits each value, skips duplicates).
         Without --path it inspects the feature's current paths read-only.

Examples:
  amber feature add --id F001 --title "User login" --priority 1 --area auth --behavior "User logs in with email" --verify "npm test" --paths src/auth
  amber feature list
  amber feature remove --id F001
  amber feature verify --feature F001 --command "npm test" --result "42 passed"
  amber feature evidence --feature F001
  amber feature paths --target . --feature F001 --path src/new.js
```

## Examples

```bash
amber feature add --id F001 --title "User login" --priority 1 --area auth --behavior "User logs in with email" --verify "npm test" --paths src/auth
```

```bash
amber feature list
```

```bash
amber feature remove --id F001
```

```bash
amber feature verify --feature F001 --command "npm test" --result "42 passed"
```

```bash
amber feature evidence --feature F001
```

```bash
amber feature paths --target . --feature F001 --path src/new.js
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
