---
id: team
title: "amber team"
sidebar_label: "team"
description: "⚠️  DEPRECATED: Inspect, install, pin, update, and roll back local team distribution metadata. Will be removed in v2."
---

import CommandBlock from '@site/src/components/command-block';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber team

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>deprecated</code> • Classification: <code>Standard Command</code>
</div>

## Summary

⚠️  DEPRECATED: Inspect, install, pin, update, and roll back local team distribution metadata. Will be removed in v2.

## Usage

```bash
amber team <inspect|install|pin|update|rollback> --target <repo> [--json]
       amber team install --target <repo> --version <version> --preset <preset> [--dry-run] [--json]
       amber team update --target <repo> --version <version> [--dry-run|--confirm] [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="read-only"
  command="amber team --target path/to/repo"
  expectedSignal="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>inspect</code> | Subcommand action for <code>team</code> |
| <code>install</code> | Subcommand action for <code>team</code> |
| <code>pin</code> | Subcommand action for <code>team</code> |
| <code>rollback</code> | Subcommand action for <code>team</code> |
| <code>update</code> | Subcommand action for <code>team</code> |

## Command Details

```text
⚠️  DEPRECATED: Inspect, install, pin, update, and roll back local team distribution metadata. Will be removed in v2.

Use install --dry-run to preview .amber/team metadata writes before creating local state.
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
