---
id: route
title: "amber route"
sidebar_label: "route"
description: "Inspect, validate, and dry-run delivery routes from routes/*.route.json."
---

import CommandBlock from '@site/src/components/CommandBlock';

<!-- Generated by scripts/lib/command-registry.js — do not edit manually. Edit scripts/lib/command-registry.js instead. -->

# amber route

<div className="amber-generated-banner">
  <strong>Reference generated from Amber Protocol v1.6.0</strong><br />
  Tier: <code>core</code> • Classification: <code>Governed Command</code>
</div>

## Summary

Inspect, validate, and dry-run delivery routes from routes/*.route.json.

## Usage

```bash
amber route <list|inspect|validate|test|approve|verify-ledger> <route-id> [--target <repo>] [--execute] [--stage <name>] [--reviewer <name>] [--json]
```

## Execution Contract

<CommandBlock
  context="Target Repository"
  nature="read-only"
  command="amber route --target path/to/repo"
  expectedOutput="Governed command execution signal"
/>

## Subcommands

| Subcommand | Description |
| --- | --- |
| <code>approve</code> | Subcommand action for <code>route</code> |
| <code>inspect</code> | Subcommand action for <code>route</code> |
| <code>list</code> | Subcommand action for <code>route</code> |
| <code>test</code> | Subcommand action for <code>route</code> |
| <code>validate</code> | Subcommand action for <code>route</code> |
| <code>verify-ledger</code> | Subcommand action for <code>route</code> |

## Command Details

```text
Inspect, validate, and dry-run delivery routes from routes/*.route.json.

Subcommands:
  list                 List all routes (id, version, stage count).
  inspect <id>         Print a route's stage tree and full JSON.
  validate <file>      Validate a route file; non-zero exit on invalid.
  test <id> --dry-run  Print the ordered stage sequence and gate points.

Examples:
  amber route list
  amber route inspect feature-standard
  amber route validate routes/feature-standard.route.json
  amber route test bugfix-quick --dry-run
```

## Examples

```bash
amber route list
```

```bash
amber route inspect feature-standard
```

```bash
amber route validate routes/feature-standard.route.json
```

```bash
amber route test bugfix-quick --dry-run
```

## Boundaries & Safety

- **Target Repository Scoping**: All mutations and reads are confined to `--target <path>`.
- **Fail-Closed Behavior**: Corrupt governance state or invalid schema parameters immediately halt execution with non-zero exit codes.
- **No Dynamic Spawning**: Command executes locally within the current process; it does not spawn unmonitored background daemons.
