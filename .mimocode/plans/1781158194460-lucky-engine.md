# Plan: Rename sample â†?sample terminology

## Goal
Replace all `sample` references in the repository with `sample` to remove the non-professional project-specific term from documentation and workflow artifacts.

## Scope
- `docs/examples/` â€?directory rename + content updates
- `scripts/amber.js` â€?help text path references
- `.workflow/` â€?file renames + content updates

## Strategy
Use bash for bulk renames and content replacements (sed/PowerShell). No code logic changes.

## Steps

### Step 1: Rename `sample-adoption-bundle/` directory
```bash
git mv docs/examples/sample-adoption-bundle docs/examples/sample-adoption-bundle
```

### Step 2: Rename files in `docs/examples/` that have `sample-` prefix
```bash
# List all files with sample- prefix in docs/examples/ (non-bundle)
git mv docs/examples/sample-adoption-*.md docs/examples/sample-adoption-*.md
```

### Step 3: Update content in `docs/examples/` files
Replace all `sample-adoption-bundle` â†?`sample-adoption-bundle` and `sample-adoption` â†?`sample-adoption` in file contents.

Key files:
- `docs/examples/README.md` â€?~10 references
- `docs/examples/sample-adoption-bundle/manifest.json` â€?8 path references
- `docs/examples/sample-adoption-bundle/README.md` â€?target path
- `docs/examples/sample-adoption-selected-files.md` â€?bundle path
- `docs/examples/sample-adoption-next-actions-cli.md` â€?bundle path
- `docs/examples/sample-adoption-decision-record.md` â€?4 bundle paths
- `docs/examples/sample-adoption-decision-record-decisions.md` â€?4 bundle paths
- `docs/examples/sample-adoption-apply-plan.md` â€?bundle path
- `docs/examples/sample-adoption-next-actions.md` â€?bundle path
- `docs/examples/sample-adoption-report.md` â€?target path
- `docs/examples/adoptions/sample-adoption-report-*.md` â€?target paths (keep filenames as-is since they're timestamped)

### Step 4: Update `scripts/amber.js`
Replace 4 help text paths: `sample-adoption-bundle` â†?`sample-adoption-bundle` and `sample-adoption-*` â†?`sample-adoption-*`

### Step 5: Rename `.workflow/` files containing `sample` in filename
39 files across ~12 workflow directories. Pattern: `packet-c-sample-docs*` â†?`packet-c-sample-docs*` and `sample-gate-resolution` directory reference.

### Step 6: Update `.workflow/` file contents
Replace `sample` â†?`sample` in all `.workflow/**/*.md` and `.workflow/**/*.json` files (~96 content matches).

## Verification
1. `grep -r "sample" --include="*.md" --include="*.json" .` should return 0 matches (excluding node_modules)
2. `npm test` â€?all tests pass (tests don't reference sample)
3. `npm run manifests` â€?manifest validation passes
4. `node scripts/amber.js doctor --target .` â€?product-repo status
