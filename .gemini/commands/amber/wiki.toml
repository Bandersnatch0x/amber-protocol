# GENERATED — edit skills/ instead. Run: npm run gen:agents
description = "Create or validate the repository-local Amber Protocol Wiki skeleton. Also manages the declarative Knowledge Plan and structured knowledge base."
prompt = """
Run the Amber amber-wiki workflow for the target repository.
If no target is provided, use the current repository root (.).
Execute: node scripts/amber.js wiki --target {{args}}
Report the command output faithfully; never overwrite user-authored files without approval.
"""
