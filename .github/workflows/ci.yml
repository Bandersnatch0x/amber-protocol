name: CI

on:
  pull_request:
  push:
    branches:
      - master
      - main
      - "codex/**"
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  test:
    name: Node ${{ matrix.node-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 18.x
          - 20.x
          - 22.x

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Validate manifests
        run: npm run manifests

      - name: Run repository doctor
        run: npm run doctor

      - name: Smoke-check CLI help
        run: node scripts/harness.js --help

  release-dry-run:
    name: Release dry-run
    runs-on: ubuntu-latest
    needs: test
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.x

      - name: Install dependencies
        run: npm install

      - name: Validate package contents
        run: npm pack --dry-run --json > npm-pack-dry-run.json

      - name: Upload package dry-run summary
        uses: actions/upload-artifact@v7
        with:
          name: npm-pack-dry-run
          path: npm-pack-dry-run.json
          archive: false
